apiVersion: v1
kind: Service
metadata:
  name: {{.Values.GatewayStream.DNS}}
spec:
  selector:
    app: {{.Values.GatewayStream.DNS}}
  ports:
    - protocol: TCP
      port: {{.Values.GatewayStream.RestPort}}
      targetPort: {{.Values.All.targetPort}}
      name: http
    - protocol: TCP
      port: {{.Values.GatewayStream.gRPCPort}}
      targetPort: {{.Values.GatewayStream.gRPCPort}}
      name: grpc
  type: LoadBalancer
  