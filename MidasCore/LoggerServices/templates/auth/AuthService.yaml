apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.AuthService.DNS }}
  labels:
    app: {{ .Values.AuthService.DNS }}
spec:
  replicas: {{ .Values.AuthService.DeploymentReplicas }}
  selector:
    matchLabels:
      app: {{ .Values.AuthService.DNS }}
  template:
    metadata:
      labels:
        app: {{ .Values.AuthService.DNS }}
    spec:
      containers:
        - name: gateway
          image: "{{ .Values.AuthService.Image }}:{{.Values.Tag}}"
          imagePullPolicy: {{ .Values.Policy.ImagePullPolicy }}
          ports:
            - containerPort: {{ .Values.AuthService.RestPort }}
              protocol: TCP
          envFrom:
            - configMapRef:
                name: app-config
            - secretRef:
                name: app-secrets
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 200m
              memory: 256Mi
          startupProbe:
            tcpSocket:
              port: {{.Values.All.targetPort}}
            failureThreshold: 30
            periodSeconds: 3
          readinessProbe:
            tcpSocket:
              port: {{.Values.All.targetPort}}
            periodSeconds: 5
            failureThreshold: 3
            timeoutSeconds: 2
          livenessProbe:
            tcpSocket:
              port: {{.Values.All.targetPort}}
            periodSeconds: 10
            failureThreshold: 5
            timeoutSeconds: 2
          {{- if .Values.AuthService.persistence.enabled }}
          volumeMounts:
            - name: auth-db
              mountPath: {{ .Values.AuthService.persistence.mountPath }}
          {{- end }}

      restartPolicy: {{ .Values.Policy.RestartPolicy }}

      {{- if .Values.AuthService.persistence.enabled }}
      volumes:
        - name: auth-db
          persistentVolumeClaim:
            claimName: {{ if .Values.AuthService.persistence.existingClaim }}{{ .Values.AuthService.persistence.existingClaim }}{{ else }}{{ .Values.AuthService.DNS }}-db{{ end }}
      {{- end }}

      securityContext:
        runAsNonRoot: false
