apiVersion: v1
kind: Service
metadata:
  name: {{.Values.SysInfoService.DNS}}
spec:
  selector:
    app: {{.Values.SysInfoService.DNS}}
  ports:
    - protocol: TCP
      port: {{.Values.SysInfoService.RestPort}}
      targetPort: {{.Values.All.targetPort}}
      name: http
  type: LoadBalancer
  